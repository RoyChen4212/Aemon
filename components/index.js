import mobile from './consumer/mobile';
import desktop from './consumer/desktop';
import { iconTypes } from './consumer/shared/icon-types';

const consumerMobile = mobile;
const consumerDesktop = desktop;

export { iconTypes, consumerDesktop, consumerMobile };
