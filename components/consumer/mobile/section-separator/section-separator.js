import React from 'react';

import './style.scss';

const baseClassName = 'pbg-consumer-mobile pbg-section-separator';

const SectionSeparator = () => <div className={baseClassName} />;

export default SectionSeparator;
